import { nodeExampleNodeLib } from './node-example-node-lib';

describe('nodeExampleNodeLib', () => {
  it('should work', () => {
    expect(nodeExampleNodeLib()).toEqual('node-example-node-lib');
  });
});
